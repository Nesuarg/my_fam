---
import '../styles/global.css';
import CoupleCard from '../components/CoupleCard.tsx';
import PersonCardGrid from '../components/PersonCardGrid.tsx';
import { buildHierarchicalFamilyTree, loadAllPeople } from '../lib/cms';
import { getAllPeople } from '../types/hierarchical-family-utils';

// Try to load from CMS first, fallback to example if no data
let foundingCouple;
let allFamilyMembers;

const familyTreeFromCms = buildHierarchicalFamilyTree();
if (familyTreeFromCms) {
  foundingCouple = familyTreeFromCms.foundingCouple;
  allFamilyMembers = getAllPeople(familyTreeFromCms);
} else {
  // Fallback: load all people from CMS without hierarchical structure
  allFamilyMembers = loadAllPeople();
  foundingCouple = null;
}
---

<html lang="da">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Samtræ for Smith Familien</title>
		<meta name="description" content="Udforsk Smith familiens historie og slægtsforbindelser" />
	</head>

	<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
		<header class="bg-white shadow-sm border-b">
			<div class="max-w-7xl mx-auto px-4 py-6">
				<h1 class="text-4xl font-bold text-center text-gray-900">
					🌳 Samtræ for Smith Familien
				</h1>
				<p class="text-center text-gray-600 mt-2">
					Udforsk vores families historie og forbindelser
				</p>
				<div class="text-center mt-4">
					<a href="/admin" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">
						👨‍💼 Administrer familie
					</a>
				</div>
			</div>
		</header>

		<main class="max-w-7xl mx-auto">
			<section class="py-8">
				<h2 class="text-2xl font-semibold text-center text-gray-800 mb-6">
					Familiemedlemmer
				</h2>
				
				{/* Display the founding couple prominently */}
				{foundingCouple && (
					<div class="mb-12">
						<h3 class="text-xl font-semibold text-center text-gray-700 mb-4">
							Stiftende Par
						</h3>
						<CoupleCard couple={foundingCouple} client:load />
					</div>
				)}

				{/* Display all family members in a grid for easy navigation */}
				{allFamilyMembers && allFamilyMembers.length > 0 ? (
					<div class="mb-8">
						<h3 class="text-xl font-semibold text-center text-gray-700 mb-4">
							Alle Familiemedlemmer ({allFamilyMembers.length})
						</h3>
						<PersonCardGrid people={allFamilyMembers} client:load />
					</div>
				) : (
					<div class="text-center py-12">
						<p class="text-gray-500 text-lg mb-4">Ingen familiemedlemmer fundet.</p>
						<a href="/admin" class="bg-blue-600 text-white px-6 py-3 rounded-md hover:bg-blue-700">
							Tilføj det første familiemedlem
						</a>
					</div>
				)}
			</section>
		</main>

		<footer class="bg-white border-t mt-16">
			<div class="max-w-7xl mx-auto px-4 py-6 text-center text-gray-600">
				<p>Bevarer Smith familiens historie for fremtidige generationer</p>
			</div>
		</footer>
	</body>
</html>
